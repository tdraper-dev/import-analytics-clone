name: "Test"

on:
  push:
    branches:
      - "main"
      - "dev"

permissions:
  actions: write
  contents: write
  id-token: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Testing
        uses: "./action"
        with:
          library: "node:fs"
          imports: "readFileSync"
